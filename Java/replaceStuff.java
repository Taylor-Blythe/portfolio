public class replaceStuff{
    public static void main (String wordToReplace, String replacer) {
            int wordCount = 0;

            for(int index = 0; index < word.length(); index++) {
                if (word.stringAt(index) == wordToReplace) {
                }
                System.out.printf("I like %s with a side of %s sauce",replacer);
            }
        }
    }
